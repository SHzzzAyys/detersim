[package]
name = "detersim-nemesis"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Deterministic fault-injection actions and connectivity models for detersim."

[dependencies]
detersim-core = { path = "../detersim-core" }
